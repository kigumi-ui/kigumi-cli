/* .Avatar {
  CSS Parts: https://webawesome.com/docs/components/avatar#css-parts
  
  &::part(icon) {
    /* The container that wraps the avatar's icon */
  }
  
  &::part(initials) {
    /* The container that wraps the avatar's initials */
  }
  
  &::part(image) {
    /* The avatar image */
  }
} */
