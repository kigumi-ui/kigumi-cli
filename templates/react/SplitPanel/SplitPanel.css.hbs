/**
 * SplitPanel Component Styles
 * Documentation: https://webawesome.com/docs/components/split-panel
 *
 * CSS Custom Properties:
 * --divider-width: Visible width of the divider line
 * --divider-hit-area: Clickable and draggable area around the divider
 * --min: Minimum size of the primary panel
 * --max: Maximum size of the primary panel
 *
 * CSS Parts:
 * start — Start (first) panel
 * end — End (second) panel
 * panel — Generic panel element used for both panels
 * divider — Draggable divider between panels
 */
.SplitPanel {
  /* Add your custom styles here */
}
