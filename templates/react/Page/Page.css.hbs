/* .Page {
  CSS Parts: https://webawesome.com/docs/components/page#css-parts

  &::part(base) {
    /* Root container for the page layout */
  }

  &::part(banner) {
    /* Banner section above header */
  }

  &::part(header) {
    /* Top-of-page header section */
  }

  &::part(subheader) {
    /* Subheader below header (e.g., breadcrumbs) */
  }

  &::part(body) {
    /* Main body container */
  }

  &::part(menu) {
    /* Menu/navigation override section */
  }

  &::part(navigation) {
    /* Primary navigation area */
  }

  &::part(navigation-header) {
    /* Navigation header section */
  }

  &::part(navigation-footer) {
    /* Navigation footer section */
  }

  &::part(navigation-toggle) {
    /* Navigation toggle button */
  }

  &::part(navigation-toggle-icon) {
    /* Navigation toggle button icon */
  }

  &::part(main-content) {
    /* Main content area */
  }

  &::part(main-header) {
    /* Inline header above main content */
  }

  &::part(main-footer) {
    /* Inline footer below main content */
  }

  &::part(aside) {
    /* Right-side aside section (e.g., table of contents) */
  }

  &::part(footer) {
    /* Page footer section */
  }

  &::part(dialog-wrapper) {
    /* Wrapper for mobile navigation drawer */
  }

  &::part(skip-links) {
    /* Skip navigation links container */
  }

  &::part(skip-link) {
    /* Individual skip navigation link */
  }
} */
