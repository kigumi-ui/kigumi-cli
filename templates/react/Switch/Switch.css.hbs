/**
 * Switch Component Styles
 * Documentation: https://webawesome.com/docs/components/switch
 *
 * CSS Custom Properties:
 * --width: Width of the switch track
 * --height: Height of the switch track
 * --thumb-size: Size of the circular thumb
 *
 * CSS Parts:
 * base — Root wrapper element
 * control — Switch track element
 * thumb — Circular thumb that slides along the track
 * label — Switch label text
 * hint — Hint text below the switch
 */
.Switch {
  /* Add your custom styles here */
}
