/**
 * ToastItem Component Styles
 * Documentation: https://webawesome.com/docs/components/toast-item
 *
 * CSS Custom Properties:
 * --accent-width: Width of the accent line
 * --show-duration: Animation duration when showing
 * --hide-duration: Animation duration when hiding
 *
 * CSS Parts:
 * toast-item — Main container
 * accent — Colored accent line on the start side
 * icon — Icon container
 * content — Message content container
 * close-button — Close button element
 * progress-ring — Progress ring component
 * close-icon — Close icon element
 */
.ToastItem {
  /* Add your custom styles here */
}
