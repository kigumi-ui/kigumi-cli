<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue';
import '{{{importPath}}}';
import './TabGroup.css';

/**
 * Tab groups organize content into a container that shows one section at a time
 */
export interface TabGroupProps {
  placement?: 'top' | 'bottom' | 'start' | 'end';
  activation?: 'auto' | 'manual';
  'without-scroll-controls'?: boolean;
}

const props = defineProps<TabGroupProps>();

const emit = defineEmits<{
  'tab-show': [event: CustomEvent];
  'tab-hide': [event: CustomEvent];
}>();

const elementRef = ref<HTMLElement | null>(null);

onMounted(() => {
  const el = elementRef.value;
  if (!el) return;

  const handleTabShow = (e: Event) => emit('tab-show', e as CustomEvent);
  el.addEventListener('wa-tab-show', handleTabShow);
  const handleTabHide = (e: Event) => emit('tab-hide', e as CustomEvent);
  el.addEventListener('wa-tab-hide', handleTabHide);

  onUnmounted(() => {
    el.removeEventListener('wa-tab-show', handleTabShow);
    el.removeEventListener('wa-tab-hide', handleTabHide);
  });
});

defineExpose({
  element: elementRef,
});
</script>

<template>
  <wa-tab-group
    ref="elementRef"
    v-bind="props"
    :class="$attrs.class"
  >
    <slot />
    <slot name="nav" />
  </wa-tab-group>
</template>
