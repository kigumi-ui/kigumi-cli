<script setup>
import { ref, onMounted, onUnmounted } from 'vue';
import '{{{importPath}}}';
import './TabGroup.css';

/**
 * Tab groups organize content into a container that shows one section at a time
 */
const props = defineProps({
    placement: { type: String, required: false, default: top },
    activation: { type: String, required: false, default: auto },
    'without-scroll-controls': { type: Boolean, required: false, default: false }
});

const emit = defineEmits(['tab-show', 'tab-hide']);

const elementRef = ref(null);

onMounted(() => {
  const el = elementRef.value;
  if (!el) return;

  const handleTabShow = (e) => emit('tab-show', e);
  el.addEventListener('wa-tab-show', handleTabShow);
  const handleTabHide = (e) => emit('tab-hide', e);
  el.addEventListener('wa-tab-hide', handleTabHide);

  onUnmounted(() => {
    el.removeEventListener('wa-tab-show', handleTabShow);
    el.removeEventListener('wa-tab-hide', handleTabHide);
  });
});

defineExpose({
  element: elementRef,
});
</script>

<template>
  <wa-tab-group
    ref="elementRef"
    v-bind="props"
    :class="$attrs.class"
  >
    <slot />
    <slot name="nav" />
  </wa-tab-group>
</template>
