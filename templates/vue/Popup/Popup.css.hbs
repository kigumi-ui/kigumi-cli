/**
 * Popup Component Styles
 * Documentation: https://webawesome.com/docs/components/popup
 *
 * CSS Custom Properties:
 * --arrow-size: Size of the popup's arrow
 * --arrow-color: Color of the popup's arrow
 * --auto-size-available-width: Available width exposed when auto-size is active
 * --auto-size-available-height: Available height exposed when auto-size is active
 * --show-duration: Duration of the show transition
 * --hide-duration: Duration of the hide transition
 *
 * CSS Parts:
 * arrow — Arrow element pointing toward the anchor
 * popup — Popup panel container
 * hover-bridge — Invisible bridge preventing pointer gaps between anchor and popup
 */
.Popup {
  /* Add your custom styles here */
}
