import { Meta } from '@storybook/addon-docs/blocks';
import { DocPage, DocSection, DocTable } from '@/components/storybook/DocTable';

<Meta title="Style/Visually Hidden" />

<DocPage
  title="Visually Hidden"
  description={
    <>
      Hide content visually while keeping it accessible to screen readers.
    </>
  }
>

<DocSection title="Default">
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'behavior', header: 'Behavior' },
      { key: 'example', header: 'Example' },
    ]}
    rows={[
      {
        cls: <code>.wa-visually-hidden</code>,
        behavior: 'Hidden from view, accessible to screen readers',
        example: (
          <div>
            <span className="wa-visually-hidden">Screen reader only text</span>
            <span style={{ color: 'var(--wa-color-text-quiet)', fontSize: 'var(--wa-font-size-s)' }}>
              Hidden above (screen reader only) — inspect the DOM to confirm.
            </span>
          </div>
        ),
      },
    ]}
  />
</DocSection>

<DocSection title="Focus Visible (Skip Links)">
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'behavior', header: 'Behavior' },
      { key: 'example', header: 'Example' },
    ]}
    rows={[
      {
        cls: <code>.wa-visually-hidden</code>,
        behavior: 'Visible on keyboard focus',
        example: (
          <div className="wa-stack">
            <span style={{ color: 'var(--wa-color-text-quiet)', fontSize: 'var(--wa-font-size-s)' }}>
              Tab into this area to reveal the skip link:
            </span>
            <a
              href="#"
              className="wa-visually-hidden"
              style={{ display: 'inline-block' }}
              onClick={(e) => e.preventDefault()}
            >
              Skip to main content
            </a>
          </div>
        ),
      },
    ]}
  />
</DocSection>

</DocPage>
