import { Meta } from '@storybook/addon-docs/blocks';
import { DocPage, DocSection, DocTable } from '@/components/storybook/DocTable';

<Meta title="Style/Native Styles" />

<DocPage
  title="Native Styles"
  description={
    <>
      When <code>utilities.css</code> is imported, Web Awesome applies
      opinionated resets to native HTML elements via{' '}
      <code>@layer wa-native</code>.
    </>
  }
>

<DocSection title="Headings">
  <DocTable
    columns={[
      { key: 'element', header: 'Element' },
      { key: 'example', header: 'Example' },
    ]}
    rows={[
      { element: <code>&lt;h1&gt;</code>, example: <h1 style={{ margin: 0 }}>Heading 1</h1> },
      { element: <code>&lt;h2&gt;</code>, example: <h2 style={{ margin: 0 }}>Heading 2</h2> },
      { element: <code>&lt;h3&gt;</code>, example: <h3 style={{ margin: 0 }}>Heading 3</h3> },
      { element: <code>&lt;h4&gt;</code>, example: <h4 style={{ margin: 0 }}>Heading 4</h4> },
      { element: <code>&lt;h5&gt;</code>, example: <h5 style={{ margin: 0 }}>Heading 5</h5> },
      { element: <code>&lt;h6&gt;</code>, example: <h6 style={{ margin: 0 }}>Heading 6</h6> },
    ]}
  />
</DocSection>

<DocSection title="Inline Elements">
  <p>
    This paragraph contains <strong>bold text</strong>, <em>italic text</em>,{' '}
    <code>inline code</code>, <a href="#">a link</a>, and{' '}
    <mark>highlighted text</mark>.
  </p>
</DocSection>

<DocSection title="Lists">
  <DocTable
    columns={[
      { key: 'type', header: 'Type' },
      { key: 'example', header: 'Example' },
    ]}
    rows={[
      {
        type: <code>Unordered</code>,
        example: (
          <ul style={{ margin: 0 }}>
            <li>Unordered item one</li>
            <li>Unordered item two</li>
            <li>Unordered item three</li>
          </ul>
        ),
      },
      {
        type: <code>Ordered</code>,
        example: (
          <ol style={{ margin: 0 }}>
            <li>Ordered item one</li>
            <li>Ordered item two</li>
            <li>Ordered item three</li>
          </ol>
        ),
      },
    ]}
  />
</DocSection>

</DocPage>
