import { Meta } from '@storybook/addon-docs/blocks';
import { DocPage, DocSection, DocTable } from '@/components/storybook/DocTable';

<Meta title="Style/Text" />

<DocPage
  title="Text"
  description={
    <>
      Typography utility classes for font size, weight, color, and text
      behavior.
    </>
  }
>

<DocSection title="Font Sizes">
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'example', header: 'Example' },
    ]}
    rows={[
      { cls: <code>.wa-font-size-xs</code>, example: <span className="wa-font-size-xs">The quick brown fox</span> },
      { cls: <code>.wa-font-size-s</code>, example: <span className="wa-font-size-s">The quick brown fox</span> },
      { cls: <code>.wa-font-size-m</code>, example: <span className="wa-font-size-m">The quick brown fox</span> },
      { cls: <code>.wa-font-size-l</code>, example: <span className="wa-font-size-l">The quick brown fox</span> },
      { cls: <code>.wa-font-size-xl</code>, example: <span className="wa-font-size-xl">The quick brown fox</span> },
      { cls: <code>.wa-font-size-2xl</code>, example: <span className="wa-font-size-2xl">The quick brown fox</span> },
    ]}
  />
</DocSection>

<DocSection title="Font Weights">
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'example', header: 'Example' },
    ]}
    rows={[
      { cls: <code>.wa-font-weight-light</code>, example: <span className="wa-font-weight-light">The quick brown fox</span> },
      { cls: <code>.wa-font-weight-normal</code>, example: <span className="wa-font-weight-normal">The quick brown fox</span> },
      { cls: <code>.wa-font-weight-semibold</code>, example: <span className="wa-font-weight-semibold">The quick brown fox</span> },
      { cls: <code>.wa-font-weight-bold</code>, example: <span className="wa-font-weight-bold">The quick brown fox</span> },
    ]}
  />
</DocSection>

<DocSection title="Text Colors">
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'description', header: 'Description' },
      { key: 'example', header: 'Example' },
    ]}
    rows={[
      {
        cls: <code>.wa-color-text-normal</code>,
        description: 'Primary body text',
        example: <span className="wa-color-text-normal">The quick brown fox</span>,
      },
      {
        cls: <code>.wa-color-text-quiet</code>,
        description: 'Secondary / de-emphasized text',
        example: <span className="wa-color-text-quiet">The quick brown fox</span>,
      },
      {
        cls: <code>.wa-color-text-link</code>,
        description: 'Interactive link text',
        example: <span className="wa-color-text-link">The quick brown fox</span>,
      },
    ]}
  />
</DocSection>

<DocSection title="Text Truncate">
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'example', header: 'Example' },
    ]}
    rows={[
      {
        cls: <code>.wa-text-truncate</code>,
        example: (
          <div
            className="wa-text-truncate"
            style={{
              width: '200px',
              border: '1px solid var(--wa-color-surface-border)',
              padding: '0.25rem 0.5rem',
              borderRadius: 'var(--wa-border-radius-s)',
            }}
          >
            This sentence is intentionally long to demonstrate text truncation behavior.
          </div>
        ),
      },
    ]}
  />
</DocSection>

</DocPage>
