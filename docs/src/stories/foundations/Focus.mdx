import { Meta, Unstyled } from '@storybook/addon-docs/blocks';
import { DocTable } from '@/components/storybook/DocTable';

<Meta title="Foundations/Focus" />

export const meta = {
  title: 'Foundations/Focus',
  tokens: [
    '--wa-focus-ring-style',
    '--wa-focus-ring-width',
    '--wa-focus-ring-offset',
    '--wa-focus-ring',
  ],
  classes: [],
};

export const FocusRingPreview = ({ style, width, offset }) => (
  <div
    style={{
      display: 'inline-block',
      width: '4rem',
      height: '4rem',
      borderRadius: 'var(--wa-border-radius-m)',
      outlineStyle: style || 'var(--wa-focus-ring-style)',
      outlineWidth: width || 'var(--wa-focus-ring-width)',
      outlineColor: 'var(--wa-color-focus)',
      outlineOffset: offset || 'var(--wa-focus-ring-offset)',
      overflow: 'hidden',
    }}
  >
    <div
      style={{
        backgroundColor: 'var(--wa-color-brand)',
        width: '100%',
        height: '100%',
        opacity: '0.33',
      }}
    />
  </div>
);

<Unstyled>
  <section className="wa-stack wa-gap-3xl">
    <div className="wa-stack wa-gap-s">
      <h1 className="wa-heading-2xl">Focus</h1>
      <p className="wa-body-l">
        Focus tokens define the visible ring that keyboard users see when
        they tab to an interactive element. Every component respects the
        same ring so navigation stays predictable. The ring color comes
        from <code>--wa-color-focus</code> on the Color page. No utility
        classes; the ring is applied automatically.
      </p>
    </div>

    <div className="wa-stack wa-gap-s">
      <h2 className="wa-heading-xl">Tokens</h2>
      <DocTable
        columns={[
          { key: 'token', header: 'Token' },
          { key: 'value', header: 'Default Value' },
          { key: 'description', header: 'Description' },
          { key: 'preview', header: 'Preview' },
        ]}
        rows={[
          {
            token: <code>--wa-focus-ring-style</code>,
            value: <code>solid</code>,
            description: 'Line style of the ring',
            preview: (
              <div className="wa-cluster wa-gap-l">
                <FocusRingPreview style="solid" />
                <FocusRingPreview style="dashed" />
                <FocusRingPreview style="dotted" />
              </div>
            ),
          },
          {
            token: <code>--wa-focus-ring-width</code>,
            value: <code>3px</code>,
            description: 'Ring thickness',
            preview: (
              <div className="wa-cluster wa-gap-l">
                <FocusRingPreview width="1px" />
                <FocusRingPreview width="3px" />
                <FocusRingPreview width="5px" />
              </div>
            ),
          },
          {
            token: <code>--wa-focus-ring-offset</code>,
            value: <code>1px</code>,
            description: 'Gap between element edge and ring',
            preview: (
              <div className="wa-cluster wa-gap-l">
                <FocusRingPreview offset="0px" />
                <FocusRingPreview offset="2px" />
                <FocusRingPreview offset="5px" />
              </div>
            ),
          },
          {
            token: <code>--wa-focus-ring</code>,
            value: 'shorthand',
            description: 'Combines style, width, and color',
            preview: <FocusRingPreview />,
          },
        ]}
      />
    </div>

  </section>
</Unstyled>
