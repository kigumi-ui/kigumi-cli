import { Meta, Unstyled } from '@storybook/addon-docs/blocks';
import { MotionSample } from '@/components/storybook/MotionSample';
import { DocTable } from '@/components/storybook/DocTable';

<Meta title="Foundations/Motion" />

export const meta = {
  title: 'Foundations/Motion',
  tokens: [
    '--wa-transition-fast',
    '--wa-transition-normal',
    '--wa-transition-slow',
    '--wa-transition-easing',
  ],
  classes: [],
};

<Unstyled>
  <section className="wa-stack wa-gap-3xl">
    <div className="wa-stack wa-gap-s">
      <h1 className="wa-heading-2xl">Motion</h1>
      <p className="wa-body-l">
        Transition tokens give every component the same motion vocabulary.
        Three durations plus one easing curve cover hover, state changes,
        and deliberate open/close animations. No utility classes; use the
        tokens directly in CSS. Components already respect{' '}
        <code>prefers-reduced-motion</code>.
      </p>
    </div>

    <div className="wa-stack wa-gap-s">
      <h2 className="wa-heading-xl">Tokens</h2>
      <p className="wa-body-l">
        Hover each preview to watch the background transition at the
        token's duration.
      </p>
      <DocTable
        columns={[
          { key: 'token', header: 'Token' },
          { key: 'value', header: 'Default Value' },
          { key: 'description', header: 'Description' },
          { key: 'preview', header: 'Preview' },
        ]}
        rows={[
          {
            token: <code>--wa-transition-fast</code>,
            value: <code>75ms</code>,
            description: 'Micro-interactions: hover, focus',
            preview: <MotionSample token="--wa-transition-fast" />,
          },
          {
            token: <code>--wa-transition-normal</code>,
            value: <code>150ms</code>,
            description: 'State changes: active, selected',
            preview: <MotionSample token="--wa-transition-normal" />,
          },
          {
            token: <code>--wa-transition-slow</code>,
            value: <code>300ms</code>,
            description: 'Deliberate shifts: open, close, checked',
            preview: <MotionSample token="--wa-transition-slow" />,
          },
          {
            token: <code>--wa-transition-easing</code>,
            value: <code>ease</code>,
            description: 'Default timing function used by every duration',
            preview: <MotionSample token="--wa-transition-slow" easing="ease" />,
          },
        ]}
      />
    </div>
  </section>
</Unstyled>
