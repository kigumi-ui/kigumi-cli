import { Meta, Unstyled } from '@storybook/addon-docs/blocks';

<Meta title="Design Tokens/Shadow" />

<Unstyled>
<div style={{ fontFamily: 'var(--wa-font-family-body)', color: 'var(--wa-color-text-normal)', maxWidth: '860px' }}>

<h1 className="wa-heading-2xl" style={{ marginBottom: '0.25rem' }}>
  Shadow Tokens
</h1>

<p style={{ marginBottom: '2rem' }}>
  Shadow tokens define the depth system. Three size tiers — small, medium, and
  large — are composed from individual offset, blur, and spread tokens.
</p>

<h2 className="wa-heading-xl">Composite Shadows</h2>

<table
  style={{
    width: '100%',
    borderCollapse: 'collapse',
    fontSize: '0.875rem',
    marginBottom: '2rem',
  }}
>
  <thead>
    <tr style={{ borderBottom: '2px solid var(--wa-color-surface-border)' }}>
      <th
        style={{
          textAlign: 'left',
          padding: '0.5rem 0.75rem',
          fontWeight: 'var(--wa-font-weight-semibold)',
        }}
      >
        Token
      </th>
      <th
        style={{
          textAlign: 'left',
          padding: '0.5rem 0.75rem',
          fontWeight: 'var(--wa-font-weight-semibold)',
        }}
      >
        Usage
      </th>
    </tr>
  </thead>
  <tbody>
    <tr style={{ borderBottom: '1px solid var(--wa-color-surface-border)' }}>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>--wa-shadow-s</code>
      </td>
      <td style={{ padding: '0.5rem 0.75rem' }}>Subtle lift (cards, inputs)</td>
    </tr>
    <tr style={{ borderBottom: '1px solid var(--wa-color-surface-border)' }}>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>--wa-shadow-m</code>
      </td>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        Moderate elevation (dropdowns, tooltips)
      </td>
    </tr>
    <tr>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>--wa-shadow-l</code>
      </td>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        High elevation (dialogs, drawers)
      </td>
    </tr>
  </tbody>
</table>

<h2 className="wa-heading-xl">Component Tokens</h2>

<table
  style={{
    width: '100%',
    borderCollapse: 'collapse',
    fontSize: '0.875rem',
    marginBottom: '1rem',
  }}
>
  <thead>
    <tr style={{ borderBottom: '2px solid var(--wa-color-surface-border)' }}>
      <th
        style={{
          textAlign: 'left',
          padding: '0.5rem 0.75rem',
          fontWeight: 'var(--wa-font-weight-semibold)',
        }}
      >
        Property
      </th>
      <th
        style={{
          textAlign: 'left',
          padding: '0.5rem 0.75rem',
          fontWeight: 'var(--wa-font-weight-semibold)',
        }}
      >
        S
      </th>
      <th
        style={{
          textAlign: 'left',
          padding: '0.5rem 0.75rem',
          fontWeight: 'var(--wa-font-weight-semibold)',
        }}
      >
        M
      </th>
      <th
        style={{
          textAlign: 'left',
          padding: '0.5rem 0.75rem',
          fontWeight: 'var(--wa-font-weight-semibold)',
        }}
      >
        L
      </th>
    </tr>
  </thead>
  <tbody>
    <tr style={{ borderBottom: '1px solid var(--wa-color-surface-border)' }}>
      <td style={{ padding: '0.5rem 0.75rem' }}>Offset Y</td>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>0.125rem</code> (2px)
      </td>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>0.25rem</code> (4px)
      </td>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>0.5rem</code> (8px)
      </td>
    </tr>
    <tr style={{ borderBottom: '1px solid var(--wa-color-surface-border)' }}>
      <td style={{ padding: '0.5rem 0.75rem' }}>Blur</td>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>0.125rem</code> (2px)
      </td>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>0.25rem</code> (4px)
      </td>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>0.5rem</code> (8px)
      </td>
    </tr>
    <tr>
      <td style={{ padding: '0.5rem 0.75rem' }}>Spread</td>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>−0.0625rem</code> (−1px)
      </td>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>−0.125rem</code> (−2px)
      </td>
      <td style={{ padding: '0.5rem 0.75rem' }}>
        <code>−0.25rem</code> (−4px)
      </td>
    </tr>
  </tbody>
</table>

<p style={{ marginBottom: '2rem' }}>
  All X offsets default to <code>0</code>. Use the scale multipliers (
  <code>--wa-shadow-offset-y-scale</code>, <code>--wa-shadow-blur-scale</code>,{' '}
  <code>--wa-shadow-spread-scale</code>) to adjust proportionally.
</p>

</div>
</Unstyled>
