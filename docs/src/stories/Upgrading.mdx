import { Meta, Unstyled } from '@storybook/addon-docs/blocks';
import { Badge, Callout, Icon } from '@/components/ui';
import { CodeBlock } from '@/components/CodeBlock';

export const initSnippet = `npx kigumi init`;

export const wrapLayoutSnippet = `import { KigumiProvider } from '@/app/providers';

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <KigumiProvider>{children}</KigumiProvider>
      </body>
    </html>
  );
}`;

export const providersGeneratedSnippet = `// app/providers.tsx (generated by kigumi init)
'use client';

import '@/lib/kigumi';

export function KigumiProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}`;

export const pagesAppSnippet = `import type { AppProps } from 'next/app';
import '@/styles/layers.css';
import '@/styles/theme.css';
import '@/lib/kigumi';

export default function App({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}`;

export const transpileSnippet = `// next.config.ts
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  transpilePackages: [
    '@awesome.me/webawesome-pro',
    '@awesome.me/webawesome',
  ],
};

export default nextConfig;`;

export const npmrcSnippet = `# .npmrc
shamefully-hoist=true`;

export const addComponentsSnippet = `npx kigumi add button card dialog input`;

export const generatedButtonSnippet = `// src/components/ui/Button/Button.tsx (generated)
'use client';

import { forwardRef, /* ... */ } from 'react';
import '@awesome.me/webawesome/dist/components/button/button.js';

export const Button = forwardRef<ButtonRef, ButtonProps>(
  function Button({ children, ...props }, ref) {
    // ...
    return (
      <wa-button ref={innerRef} suppressHydrationWarning {...props}>
        {children}
      </wa-button>
    );
  }
);`;

export const regenerateSnippet = `npx kigumi update --force`;

<Meta title="General / Upgrading" />

<Unstyled>
  <div className="wa-stack wa-gap-4xl">
    <div className="wa-stack wa-gap-xl">
      <h1 className="wa-heading-3xl" id="upgrading">Upgrading</h1>
      <p className="wa-body-l">
        Migration notes for Kigumi CLI releases. Kigumi is pre-1.0, so
        minor-version bumps (0.x.0) may carry breaking changes per SemVer.
        Releases without breaking changes are listed for completeness.
      </p>
    </div>

    <div className="wa-stack wa-gap-xl">
      <h2 className="wa-heading-2xl" id="v0-20">Upgrading to 0.20 — Next.js App Router</h2>

      <Callout variant="brand">
        <Icon slot="icon" name="circle-info" />
        <p>
          <strong>No breaking changes.</strong> Existing Vite + React projects
          work unchanged. This release teaches <code>kigumi init</code> and
          the template generator to recognize Next.js projects — both App
          Router and Pages Router — and scaffold them correctly.
        </p>
      </Callout>

      <div className="wa-stack wa-gap-m">
        <div className="wa-cluster wa-gap-xs wa-align-items-center">
          <Badge pill>1</Badge>
          <h3 className="wa-heading-l" id="next-init">Initialize inside a Next.js project</h3>
        </div>
        <p className="wa-caption-m">
          From a Next.js project root (React 18 or 19), run{' '}
          <code>kigumi init</code>. The CLI detects Next by checking{' '}
          <code>package.json</code> for <code>next</code> or the presence of{' '}
          <code>next.config.&#123;js,mjs,ts,cjs&#125;</code>, then adapts the
          scaffold to the router you use and the project's layout:
        </p>
        <CodeBlock language="bash" value={initSnippet} />
        <ul>
          <li>
            <strong>App Router</strong> (<code>app/</code> or{' '}
            <code>src/app/</code>): writes <code>providers.tsx</code> next to
            <code>app/</code> with a <code>KigumiProvider</code> Client Module
            so your root layout can stay a Server Component.
          </li>
          <li>
            <strong>Pages Router</strong> (<code>pages/</code>): does not
            scaffold a new file. Your <code>pages/_app.tsx</code> is yours;
            Kigumi prints the one-line import you need to add (see step 2b).
          </li>
          <li>
            Skips <code>vite.config.ts</code> and <code>vite-env.d.ts</code>{' '}
            writes; emits a sibling <code>web-awesome.d.ts</code> without the
            Vite type reference.
          </li>
          <li>
            Matches directory defaults to your layout: projects with{' '}
            <code>src/</code> get <code>src/components/ui</code> /{' '}
            <code>src/lib</code> / <code>src/styles</code>; projects without
            <code>src/</code> (e.g. <code>create-next-app</code> without{' '}
            <code>--src-dir</code>) get the parallel <code>components/ui</code>
            {' '}/ <code>lib</code> / <code>styles</code> layout so your
            existing <code>@/*</code> path alias resolves without edits.
          </li>
          <li>
            Falls back to <code>tsconfig.json</code> for path-alias
            configuration when <code>tsconfig.app.json</code> is absent.
          </li>
        </ul>
        <p className="wa-caption-m">Generated App Router <code>providers.tsx</code>:</p>
        <CodeBlock language="tsx" value={providersGeneratedSnippet} />
      </div>

      <div className="wa-stack wa-gap-m">
        <div className="wa-cluster wa-gap-xs wa-align-items-center">
          <Badge pill>2a</Badge>
          <h3 className="wa-heading-l" id="wrap-root-layout">Wire Kigumi — App Router</h3>
        </div>
        <p className="wa-caption-m">
          <code>app/layout.tsx</code> stays a Server Component. Import{' '}
          <code>KigumiProvider</code> from the generated providers module and
          wrap <code>&#123;children&#125;</code>:
        </p>
        <CodeBlock language="tsx" value={wrapLayoutSnippet} />
        <p className="wa-caption-m">
          The provider pulls <code>@/lib/kigumi</code> into the Client bundle
          so Web Awesome registers once per browser session, while your
          layout, metadata, and data fetching remain server-rendered.
        </p>
      </div>

      <div className="wa-stack wa-gap-m">
        <div className="wa-cluster wa-gap-xs wa-align-items-center">
          <Badge pill>2b</Badge>
          <h3 className="wa-heading-l" id="wire-pages">Wire Kigumi — Pages Router</h3>
        </div>
        <p className="wa-caption-m">
          Pages Router has no RSC boundary, so there's no provider to scaffold.
          Add three imports to <code>pages/_app.tsx</code>:
        </p>

        <Callout variant="warning">
          <Icon slot="icon" name="triangle-exclamation" />
          <div className="wa-stack wa-gap-s">
            <p>
              <strong>Environment setup.</strong> Pages Router needs two
              config changes before the imports work.
            </p>
            <ol>
              <li>
                <p>
                  Add <code>transpilePackages</code> to your{' '}
                  <code>next.config.js</code>/<code>next.config.ts</code>.
                  Pages Router SSR uses Node ESM resolution, which does not
                  follow pnpm's symlink graph for Lit. Without this entry,{' '}
                  <code>next dev</code> crashes with{' '}
                  <code>Cannot find package 'lit'</code>.
                </p>
                <CodeBlock language="ts" value={transpileSnippet} />
              </li>
              <li>
                <p>
                  <strong>pnpm users:</strong> add{' '}
                  <code>shamefully-hoist=true</code> to the project's{' '}
                  <code>.npmrc</code>. Next's Pages Router SSR pipeline holds
                  a single React instance, but pnpm's default isolated{' '}
                  <code>node_modules</code> layout leaks multiple React
                  resolution paths through <code>styled-jsx</code> and other
                  Next dev-overlay deps. Symptom:{' '}
                  <code>
                    Cannot read properties of null (reading 'useContext' |
                    'useReducer' | 'useInsertionEffect')
                  </code>{' '}
                  during SSR.
                </p>
                <CodeBlock language="ini" value={npmrcSnippet} />
              </li>
            </ol>
          </div>
        </Callout>

        <CodeBlock language="tsx" value={pagesAppSnippet} />
        <p className="wa-caption-m">
          Next's Pages Router enforces that global CSS can only be imported
          from <code>_app.tsx</code> directly — not transitively from
          <code>lib/</code> or a component file. Kigumi detects Pages Router
          at generation time and{' '}
          <strong>
            omits the <code>layers.css</code> import from{' '}
            <code>lib/kigumi.ts</code> and the per-component{' '}
            <code>.css</code> imports from each wrapper
          </strong>
          , so the build succeeds. The trade-off is that{' '}
          <code>_app.tsx</code> owns all CSS imports. If you customize the
          stub <code>Button.css</code> / <code>Card.css</code> etc. files,
          add their imports to <code>_app.tsx</code> manually. No{' '}
          <code>'use client'</code> needed in your <code>_app.tsx</code>
          {' '}(Pages Router code runs in both server and client contexts by
          default).
        </p>
      </div>

      <div className="wa-stack wa-gap-m">
        <div className="wa-cluster wa-gap-xs wa-align-items-center">
          <Badge pill>3</Badge>
          <h3 className="wa-heading-l" id="add-components">Add components</h3>
        </div>
        <p className="wa-caption-m">
          Same command for both routers:
        </p>
        <CodeBlock language="bash" value={addComponentsSnippet} />
        <p className="wa-caption-m">
          Wrappers land in your configured <code>componentsDir</code> and can
          be imported from any Server or Client Component (App Router) or
          page (Pages Router). Every generated wrapper carries a leading{' '}
          <code>'use client';</code>: in App Router this promotes it to a
          Client Module automatically, in Pages Router Next treats it as a
          harmless top-level string literal.
        </p>
      </div>

      <div className="wa-stack wa-gap-m">
        <h3 className="wa-heading-l" id="under-the-hood">What changes in the generated code</h3>
        <p className="wa-caption-m">
          The React template generator branches on Next detection. Two changes
          are worth knowing about if you read the generated output:
        </p>

        <p><strong>1. <code>'use client';</code> directive.</strong> Injected on line 1 of every generated React wrapper and of <code>src/lib/kigumi.ts</code>. Without it, Next would try to server-render the custom element reference and fail because Web Awesome's Lit runtime depends on browser APIs.</p>

        <p><strong>2. <code>suppressHydrationWarning</code> on the host element.</strong> Lit-based Web Awesome components reflect default attributes to the DOM during <code>connectedCallback</code> (e.g., <code>appearance="outlined"</code> on{' '}<code>&lt;wa-button&gt;</code>,{' '}<code>library="default"</code> on{' '}<code>&lt;wa-icon&gt;</code>). React's SSR output does not include those, so without suppression every page that mounts a Kigumi component throws a hydration-mismatch warning. This is the documented React API for elements whose attributes are mutated by a runtime after hydration — it suppresses only the host element (children are still hydration-checked), and it's a no-op in non-SSR contexts, so Vite projects are not affected.</p>

        <p>Example (abbreviated):</p>
        <CodeBlock language="tsx" value={generatedButtonSnippet} />
      </div>

      <div className="wa-stack wa-gap-m">
        <h3 className="wa-heading-l" id="vite-projects">Existing Vite projects</h3>
        <p className="wa-caption-m">
          Nothing to do. Kigumi only treats a project as Next when{' '}
          <code>isNextProject(cwd)</code> returns true; otherwise template
          generation and init behave exactly as in 0.19. Your{' '}
          <code>vite.config.ts</code>, <code>vite-env.d.ts</code>, and{' '}
          <code>main.tsx</code> entry stay untouched.
        </p>
      </div>

      <div className="wa-stack wa-gap-m">
        <h3 className="wa-heading-l" id="regenerating">Regenerating existing Kigumi components in a Next project</h3>
        <p className="wa-caption-m">
          If you adopted Kigumi on 0.19 inside a Next project (with manual
          workarounds for hydration warnings) and are upgrading to 0.20, rerun
          the generator against your existing installation:
        </p>
        <CodeBlock language="bash" value={regenerateSnippet} />
        <p className="wa-caption-m">
          <code>kigumi update</code> does a three-way merge by default —
          local edits are preserved where possible, with conflict markers
          where they collide. <code>--force</code> takes the new template
          output verbatim, which is what you want here so the{' '}
          <code>'use client';</code> and <code>suppressHydrationWarning</code>{' '}
          changes land everywhere.
        </p>
      </div>

      <div className="wa-stack wa-gap-m">
        <h3 className="wa-heading-l" id="not-supported">Known limitations</h3>
        <ul>
          <li>
            <strong>Server-rendered Web Awesome output.</strong> Custom
            elements render as empty <code>&lt;wa-*&gt;</code> tags during
            SSR and hydrate on the client. Visible output appears after
            hydration. This is a fundamental property of custom elements
            without Declarative Shadow DOM — not a Kigumi-specific
            limitation. For fully-server-rendered shadow output, Web
            Awesome's Pro SSR loader is the intended path.
          </li>
        </ul>
      </div>
    </div>
  </div>
</Unstyled>
