import { Meta } from '@storybook/addon-docs/blocks';
import { DocPage, DocSection, DocTable } from '@/components/storybook/DocTable';

<Meta title="Layout / Stack" />

export const StackBars = ({ className, style }) => (
  <div
    className={className || 'wa-stack'}
    style={{
      maxWidth: '200px',
      border: '1px solid var(--wa-color-surface-border)', borderRadius: 'var(--wa-border-radius-m)',
      ...style,
    }}
  >
    <div style={{ backgroundColor: 'var(--wa-color-brand)', width: '100%', height: '2rem' }} />
    <div style={{ backgroundColor: 'var(--wa-color-brand)', width: '80%', height: '2rem' }} />
    <div style={{ backgroundColor: 'var(--wa-color-brand)', width: '60%', height: '2rem' }} />
  </div>
);

<DocPage
  title="Stack"
  description={
    <>
      Arranges children vertically with consistent spacing via{' '}
      <code>.wa-stack</code>. Well suited for forms, text blocks, and vertical
      content flow.
    </>
  }
>

<DocSection title="CSS Classes">
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'desc', header: 'Description' },
      { key: 'preview', header: 'Preview' },
    ]}
    rows={[
      {
        cls: <code>.wa-stack</code>,
        desc: 'Vertical flex column with default gap',
        preview: <StackBars />,
      },
    ]}
  />
</DocSection>

</DocPage>
