import { Meta } from '@storybook/addon-docs/blocks';
import { DocPage, DocSection, DocTable } from '@/components/storybook/DocTable';

<Meta title="Layout / Justify Content" />

<DocPage
  title="Justify Content"
  description={<>Distributes space along the main axis of flex and grid containers via <code>.wa-justify-content-*</code> classes. These classes also implicitly set <code>display: flex</code>.</>}
>

<DocSection title="CSS Classes">
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'preview', header: 'Preview' },
    ]}
    rows={[
      {
        cls: <code>.wa-justify-content-start</code>,
        className: 'wa-justify-content-start',
      },
      {
        cls: <code>.wa-justify-content-end</code>,
        className: 'wa-justify-content-end',
      },
      {
        cls: <code>.wa-justify-content-center</code>,
        className: 'wa-justify-content-center',
      },
      {
        cls: <code>.wa-justify-content-space-around</code>,
        className: 'wa-justify-content-space-around',
      },
      {
        cls: <code>.wa-justify-content-space-between</code>,
        className: 'wa-justify-content-space-between',
      },
      {
        cls: <code>.wa-justify-content-space-evenly</code>,
        className: 'wa-justify-content-space-evenly',
      },
    ].map((row) => ({
      cls: row.cls,
      preview: (
        <div
          className={row.className}
          style={{
            minWidth: '260px',
            border: '1px solid var(--wa-color-surface-border)',
            borderRadius: 'var(--wa-border-radius-m)',
          }}
        >
          {[0, 1, 2].map((i) => (
            <div
              key={i}
              style={{
                backgroundColor: 'var(--wa-color-brand)',
                width: '2rem',
                height: '2rem',
              }}
            />
          ))}
        </div>
      ),
    }))}
  />
</DocSection>

</DocPage>
