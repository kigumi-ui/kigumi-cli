import { Meta } from '@storybook/addon-docs/blocks';
import { DocPage, DocSection, DocTable } from '@/components/storybook/DocTable';

<Meta title="Layout / Flank" />

export const FlankDemo = ({ className, style }) => (
  <div
    className={className || 'wa-flank'}
    style={{
      minWidth: '16em',
      border: '2px solid var(--wa-color-surface-border)',
      ...style,
    }}
  >
    <span
      style={{
        backgroundColor: 'var(--wa-color-brand-fill-loud)',
        height: '1em',
        width: '1em',
      }}
    />
    <span
      style={{
        backgroundColor: 'var(--wa-color-brand-fill-normal)',
        height: '1em',
      }}
    />
  </div>
);

export const FlankDemoEnd = ({ className, style }) => (
  <div
    className={className || 'wa-flank:end'}
    style={{
      minWidth: '16em',
      border: '2px solid var(--wa-color-surface-border)',
      ...style,
    }}
  >
    <span
      style={{
        backgroundColor: 'var(--wa-color-brand-fill-normal)',
        height: '1em',
      }}
    />
    <span
      style={{
        backgroundColor: 'var(--wa-color-brand-fill-loud)',
        height: '1em',
        width: '1em',
      }}
    />
  </div>
);

<DocPage
  title="Flank"
  description={
    <>
      Positions one item alongside content that fills the remaining space via{' '}
      <code>.wa-flank</code>. Items wrap when space is limited. Useful for
      asides, search bars, and media + text layouts.
    </>
  }
>

<DocSection title="CSS Classes">
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'desc', header: 'Description' },
      { key: 'example', header: 'Examples' },
    ]}
    rows={[
      {
        cls: <code>.wa-flank</code>,
        desc: 'First child flanks, second child fills remaining space',
        example: <FlankDemo />,
      },
    ]}
  />
</DocSection>

<DocSection
  title="Position"
  description={
    <>
      Controls which child acts as the flanking element. Append{' '}
      <code>:start</code> (default) or <code>:end</code> to specify whether the
      first or last child flanks the content.
    </>
  }
>
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'desc', header: 'Description' },
      { key: 'example', header: 'Examples' },
    ]}
    rows={[
      {
        cls: <code>.wa-flank:start</code>,
        desc: 'First child flanks (default)',
        example: <FlankDemo className="wa-flank:start" />,
      },
      {
        cls: <code>.wa-flank:end</code>,
        desc: 'Last child flanks',
        example: <FlankDemoEnd />,
      },
    ]}
  />
</DocSection>

<DocSection
  title="Sizing"
  description={
    <>
      The flanking element sizes to its content by default. Use{' '}
      <code>--flank-size</code> to set a specific width, and{' '}
      <code>--content-percentage</code> to control the minimum width of the main
      content before wrapping occurs.
    </>
  }
>
  <DocTable
    columns={[
      { key: 'property', header: 'Property' },
      { key: 'desc', header: 'Description' },
      { key: 'example', header: 'Examples' },
    ]}
    rows={[
      {
        property: <code>--flank-size</code>,
        desc: 'Sets the target width of the flanking element',
        example: (
          <div
            className="wa-flank"
            style={{
              '--flank-size': '6rem',
              minWidth: '16em',
              border: '2px solid var(--wa-color-surface-border)',
            }}
          >
            <span
              style={{
                backgroundColor: 'var(--wa-color-brand-fill-loud)',
                height: '1em',
              }}
            />
            <span
              style={{
                backgroundColor: 'var(--wa-color-brand-fill-normal)',
                height: '1em',
              }}
            />
          </div>
        ),
      },
      {
        property: <code>--content-percentage</code>,
        desc: (
          <>
            Minimum width of main content before wrapping (default{' '}
            <code>50%</code>)
          </>
        ),
        example: (
          <div
            className="wa-flank"
            style={{
              '--content-percentage': '70%',
              minWidth: '16em',
              border: '2px solid var(--wa-color-surface-border)',
            }}
          >
            <span
              style={{
                backgroundColor: 'var(--wa-color-brand-fill-loud)',
                height: '1em',
                width: '1em',
              }}
            />
            <span
              style={{
                backgroundColor: 'var(--wa-color-brand-fill-normal)',
                height: '1em',
              }}
            />
          </div>
        ),
      },
    ]}
  />
</DocSection>

<DocSection
  title="Align Items"
  description={
    <>
      Items are vertically centered by default. Add a{' '}
      <code>.wa-align-items-*</code> class to change cross-axis alignment.
    </>
  }
>
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'example', header: 'Examples' },
    ]}
    rows={[
      {
        cls: <code>.wa-align-items-start</code>,
        example: (
          <div
            className="wa-flank wa-align-items-start"
            style={{
              minWidth: '16em',
              minHeight: '3em',
              border: '2px solid var(--wa-color-surface-border)',
            }}
          >
            <span
              style={{
                backgroundColor: 'var(--wa-color-brand-fill-loud)',
                height: '1em',
                width: '1em',
              }}
            />
            <span
              style={{
                backgroundColor: 'var(--wa-color-brand-fill-normal)',
                height: '1em',
              }}
            />
          </div>
        ),
      },
      {
        cls: <code>.wa-align-items-end</code>,
        example: (
          <div
            className="wa-flank wa-align-items-end"
            style={{
              minWidth: '16em',
              minHeight: '3em',
              border: '2px solid var(--wa-color-surface-border)',
            }}
          >
            <span
              style={{
                backgroundColor: 'var(--wa-color-brand-fill-loud)',
                height: '1em',
                width: '1em',
              }}
            />
            <span
              style={{
                backgroundColor: 'var(--wa-color-brand-fill-normal)',
                height: '1em',
              }}
            />
          </div>
        ),
      },
      {
        cls: <code>.wa-align-items-center</code>,
        example: (
          <div
            className="wa-flank wa-align-items-center"
            style={{
              minWidth: '16em',
              minHeight: '3em',
              border: '2px solid var(--wa-color-surface-border)',
            }}
          >
            <span
              style={{
                backgroundColor: 'var(--wa-color-brand-fill-loud)',
                height: '1em',
                width: '1em',
              }}
            />
            <span
              style={{
                backgroundColor: 'var(--wa-color-brand-fill-normal)',
                height: '1em',
              }}
            />
          </div>
        ),
      },
      {
        cls: <code>.wa-align-items-stretch</code>,
        example: (
          <div
            className="wa-flank wa-align-items-stretch"
            style={{
              minWidth: '16em',
              height: '3em',
              border: '2px solid var(--wa-color-surface-border)',
            }}
          >
            <span
              style={{
                backgroundColor: 'var(--wa-color-brand-fill-loud)',
                width: '1em',
              }}
            />
            <span
              style={{ backgroundColor: 'var(--wa-color-brand-fill-normal)' }}
            />
          </div>
        ),
      },
    ]}
  />
</DocSection>

<DocSection
  title="Gap"
  description={
    <>
      The default gap between flank items uses <code>--wa-space-m</code>. Add a{' '}
      <code>.wa-gap-*</code> class to adjust.
    </>
  }
>
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'example', header: 'Examples' },
    ]}
    rows={[
      {
        cls: <code>.wa-gap-2xs</code>,
        example: <FlankDemo className="wa-flank wa-gap-2xs" />,
      },
      {
        cls: <code>.wa-gap-m</code>,
        example: <FlankDemo className="wa-flank wa-gap-m" />,
      },
      {
        cls: <code>.wa-gap-2xl</code>,
        example: <FlankDemo className="wa-flank wa-gap-2xl" />,
      },
    ]}
  />
</DocSection>

</DocPage>
