import { Meta } from '@storybook/addon-docs/blocks';
import { DocPage, DocSection, DocTable } from '@/components/storybook/DocTable';

<Meta title="Layout / Split" />

export const SplitPair = ({ className, style }) => (
  <div
    className={className || 'wa-split'}
    style={{
      minWidth: '16rem',
      border: '1px solid var(--wa-color-surface-border)', borderRadius: 'var(--wa-border-radius-m)',
      ...style,
    }}
  >
    <div style={{ backgroundColor: 'var(--wa-color-brand)', width: '2rem', height: '2rem' }} />
    <div style={{ backgroundColor: 'var(--wa-color-brand)', width: '2rem', height: '2rem', opacity: 0.6 }} />
  </div>
);

<DocPage
  title="Split"
  description={
    <>
      Distributes space evenly between two or more children via{' '}
      <code>.wa-split</code>. Useful for headers, footers, and navigation
      layouts where items need maximum separation.
    </>
  }
>

<DocSection title="CSS Classes">
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'desc', header: 'Description' },
      { key: 'preview', header: 'Preview' },
    ]}
    rows={[
      {
        cls: <code>.wa-split</code>,
        desc: 'Distributes children with space-between',
        preview: <SplitPair />,
      },
    ]}
  />
</DocSection>

<DocSection
  title="Direction"
  description={
    <>
      Append <code>:row</code> or <code>:column</code> to control whether items
      split horizontally or vertically. Row is the default.
    </>
  }
>
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'desc', header: 'Description' },
      { key: 'preview', header: 'Preview' },
    ]}
    rows={[
      {
        cls: <code>.wa-split:row</code>,
        desc: 'Horizontal split (default)',
        preview: <SplitPair className="wa-split:row" />,
      },
      {
        cls: <code>.wa-split:column</code>,
        desc: 'Vertical split',
        preview: (
          <div
            className="wa-split:column"
            style={{
              width: '2rem',
              height: '6rem',
              border: '1px solid var(--wa-color-surface-border)', borderRadius: 'var(--wa-border-radius-m)',
            }}
          >
            <div style={{ backgroundColor: 'var(--wa-color-brand)', width: '100%', height: '2rem' }} />
            <div style={{ backgroundColor: 'var(--wa-color-brand)', width: '100%', height: '2rem', opacity: 0.6 }} />
          </div>
        ),
      },
    ]}
  />
</DocSection>

</DocPage>
