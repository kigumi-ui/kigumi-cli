import { Meta } from '@storybook/addon-docs/blocks';
import { DocPage, DocSection, DocTable } from '@/components/storybook/DocTable';

<Meta title="Layout / Flex Wrap" />

export const WrapSquares = ({ className }) => (
  <>
    <div
      style={{
        backgroundColor: 'var(--wa-color-brand-fill-loud)',
        width: '3em',
        height: '3em',
      }}
    />
    <div
      style={{
        backgroundColor: 'var(--wa-color-brand-fill-normal)',
        width: '3em',
        height: '3em',
      }}
    />
    <div
      style={{
        backgroundColor: 'var(--wa-color-brand-fill-quiet)',
        width: '3em',
        height: '3em',
      }}
    />
  </>
);

<DocPage
  title="Flex Wrap"
  description={
    <>
      Controls whether flex items wrap onto multiple lines via{' '}
      <code>.wa-flex-wrap</code>, <code>.wa-flex-nowrap</code>, and{' '}
      <code>.wa-flex-wrap-reverse</code> classes. These classes also implicitly
      set <code>display: flex</code>.
    </>
  }
>

<DocSection title="CSS Classes">
  <DocTable
    columns={[
      { key: 'cls', header: 'Class' },
      { key: 'desc', header: 'Description' },
      { key: 'example', header: 'Examples' },
    ]}
    rows={[
      {
        cls: <code>.wa-flex-wrap</code>,
        desc: 'Items wrap onto additional lines when needed',
        example: (
          <div
            style={{
              maxWidth: '8em',
              border: '2px solid var(--wa-color-surface-border)',
            }}
          >
            <div className="wa-flex-wrap wa-gap-xs">
              <WrapSquares />
            </div>
          </div>
        ),
      },
      {
        cls: <code>.wa-flex-nowrap</code>,
        desc: 'Items stay on a single line, may overflow',
        example: (
          <div
            style={{
              maxWidth: '8em',
              border: '2px solid var(--wa-color-surface-border)',
              overflow: 'auto',
            }}
          >
            <div className="wa-flex-nowrap wa-gap-xs">
              <WrapSquares />
            </div>
          </div>
        ),
      },
      {
        cls: <code>.wa-flex-wrap-reverse</code>,
        desc: 'Items wrap in reverse order onto preceding lines',
        example: (
          <div
            style={{
              maxWidth: '8em',
              border: '2px solid var(--wa-color-surface-border)',
            }}
          >
            <div className="wa-flex-wrap-reverse wa-gap-xs">
              <WrapSquares />
            </div>
          </div>
        ),
      },
    ]}
  />
</DocSection>

</DocPage>
